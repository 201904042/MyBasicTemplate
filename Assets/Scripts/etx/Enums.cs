public enum ObjectType
{
    Test,
    Player,
    Enemy,
    Projectile,
    Environment
}

public enum UIType
{
    Test,
    Loading,
    MainMenu,
    Settings,
}

public enum SoundType
{
    Test
}

public enum SpriteType
{
    Test
}